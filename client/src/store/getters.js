export const character = state => (state.character);
export const characterRelationships = state => (state.characterRelationships);
export const universe = state => (state.universe);
export const universes = state => (state.universes);
export const user = state => (state.user);
