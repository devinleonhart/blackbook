<section id="slideshows-show-view" class="bb-page">
  <% render partial: "shared/capture_navbar" %>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-gray-900">Slideshow</h1>
      <p class="text-gray-600">Space = play/pause, ←/→ = prev/next.</p>
    </div>

    <% unless @has_images %>
      <div class="bg-white rounded-lg p-6 shadow-lg">
        <%= render partial: "shared/empty_banner", locals: { message: (@mode == "favorites" ? "No favorite images yet. Favorite an image to see it here." : "No images available for slideshow.") } %>
      </div>
    <% else %>
      <div
        class="bb-slideshow-shell"
        data-controller="slideshow"
        data-slideshow-slides-url-value="<%= slideshow_images_path(mode: @mode, universe_id: @universe_id) %>"
        data-slideshow-interval-ms-value="3000"
      >
        <!-- Top controls -->
        <div class="bb-slideshow-controls bb-slideshow-controls--top" data-slideshow-target="topControls">
          <div class="flex flex-wrap items-center justify-between gap-3">
            <div class="flex flex-wrap items-center gap-2">
              <%= form_with url: slideshow_path, method: :get, local: true, class: "flex items-center" do %>
                <%= hidden_field_tag :mode, @mode %>
                <select
                  id="slideshow_universe_id"
                  name="universe_id"
                  class="bb-input w-auto"
                  onchange="this.form.requestSubmit()"
                  aria-label="Universe"
                >
                  <option value="" <%= @universe_id.nil? ? "selected" : "" %>>All universes</option>
                  <% @universes.each do |universe| %>
                    <option value="<%= universe.id %>" <%= universe.id == @universe_id ? "selected" : "" %>><%= universe.name %></option>
                  <% end %>
                </select>
              <% end %>

              <%= link_to "All",
                          slideshow_path(mode: "all", universe_id: @universe_id),
                          class: "px-4 py-2 rounded border text-sm font-medium transition-colors " \
                                 "#{@mode == "favorites" ? "bg-white hover:bg-gray-50 active:bg-gray-100 border-gray-300 text-gray-900" : "bg-blue-600 active:bg-blue-800 border-blue-600 text-white"}" %>
              <%= link_to "Favorites",
                          slideshow_path(mode: "favorites", universe_id: @universe_id),
                          class: "px-4 py-2 rounded border text-sm font-medium transition-colors " \
                                 "#{@mode == "favorites" ? "bg-blue-600 active:bg-blue-800 border-blue-600 text-white" : "bg-white hover:bg-gray-50 active:bg-gray-100 border-gray-300 text-gray-900"}" %>
              <button type="button" class="px-4 py-2 rounded bg-white hover:bg-gray-50 active:bg-gray-100 border border-gray-300 text-gray-900 text-sm font-medium transition-colors" data-action="slideshow#prev">Prev</button>
              <button type="button" class="px-4 py-2 rounded bg-blue-500 hover:bg-blue-700 active:bg-blue-800 text-white text-sm font-medium transition-colors" data-action="slideshow#togglePlay" data-slideshow-target="playButton">Play</button>
              <button type="button" class="px-4 py-2 rounded bg-white hover:bg-gray-50 active:bg-gray-100 border border-gray-300 text-gray-900 text-sm font-medium transition-colors" data-action="slideshow#next">Next</button>
              <button type="button" class="px-4 py-2 rounded bg-white hover:bg-gray-50 active:bg-gray-100 border border-gray-300 text-gray-900 text-sm font-medium transition-colors" data-action="slideshow#toggleFullscreen" data-slideshow-target="fullscreenButton">Fullscreen</button>
            </div>

            <span class="text-sm text-gray-700" data-slideshow-target="counter"></span>
          </div>
        </div>

        <!-- Stage -->
        <div
          class="bb-slideshow-stage"
          data-slideshow-target="stage"
          data-action="click->slideshow#stageTapped"
          tabindex="0"
        >
          <img
            data-slideshow-target="image"
            src=""
            alt="Slideshow image"
            class="bb-slideshow-image bb-slideshow-image--default"
          />

          <div data-slideshow-target="loading" class="absolute inset-0 flex items-center justify-center text-white text-sm bg-black/40">
            Loading…
          </div>

          <div data-slideshow-target="error" class="hidden absolute top-4 left-1/2 -translate-x-1/2 bg-red-600/90 text-white text-sm px-3 py-2 rounded">
            <span data-slideshow-target="errorText">Failed to load slideshow.</span>
          </div>

          <div data-slideshow-target="fullscreenHint" class="hidden absolute top-4 left-1/2 -translate-x-1/2 bg-black/60 text-white text-sm px-3 py-2 rounded">
            Fullscreen — press Esc to exit
          </div>
        </div>

        <!-- Bottom controls -->
        <div class="bb-slideshow-controls bb-slideshow-controls--bottom" data-slideshow-target="bottomControls">
          <div class="flex items-center justify-between">
            <label class="text-sm font-medium text-gray-700" for="slideshow_speed">Speed</label>
            <span class="text-sm text-gray-700 w-16 text-right" data-slideshow-target="speedLabel">3.0s</span>
          </div>
          <input id="slideshow_speed" type="range" min="1" max="300" step="1" value="3" class="w-full mt-2" data-action="input->slideshow#speedChanged" data-slideshow-target="speed" />
        </div>
      </div>
    <% end %>
  </div>
</section>
