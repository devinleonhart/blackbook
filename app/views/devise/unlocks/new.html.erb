<section id="bb_unlocks_new" class="bb-page">
  <div class="bb-container-xs">
    <div class="flex justify-center">
      <div class="w-full">
        <div class="bb-page-header">
          <h1 class="bb-heading-xl">Resend unlock instructions</h1>
          <p class="bb-subtitle">We'll email you a link to unlock your account.</p>
        </div>

        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "bb-card bb-card--form" }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="bb-field-lg">
            <%= f.label :email, class: "bb-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bb-input" %>
          </div>

          <div class="flex items-center justify-between">
            <%= f.submit "Resend unlock instructions", class: "bb-btn bb-btn-primary bb-btn--full" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>
