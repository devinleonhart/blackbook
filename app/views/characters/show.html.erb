<section id="characters-show-view" class="section">

  <% render partial: "shared/capture_navbar" %>

  <div class="columns is-centered">
    <div class="column">
      <%= render partial: "shared/image_list", locals: {
        images: @images,
        universe: @character.universe
      } %>
    </div>
  </div>

</section>
