<section class="characters-show-view">

  <section class="character-content">
    <h1><%= @character.name %></h1>
  </section>

  <% render partial: "shared/capture_navbar" %>

  <%= render partial: "shared/image_list", locals: {
    images: @images,
    universe: @character.universe,
  } %>

</section>
