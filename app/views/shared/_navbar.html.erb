<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Blackbook</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <ul class="navbar-nav">
      <% unless @character.nil? %>
        <%= render partial: "shared/navbar_link", locals: {
          label: "Back to #{@character.universe.name}",
          url: universe_url(@character.universe),
        } %>
      <% end %>
      <% unless @image.nil? %>
        <%= render partial: "shared/navbar_link", locals: {
          label: "Back to #{@image.universe.name}",
          url: universe_url(@image.universe),
        } %>
      <% end %>
      <% unless @location.nil? %>
        <%= render partial: "shared/navbar_link", locals: {
          label: "Back to #{@location.universe.name}",
          url: universe_url(@location.universe),
        } %>
      <% end %>
    </ul>
    <div class="collapse navbar-collapse flex-row-reverse" id="navbarNav">
      <ul class="navbar-nav">
        <% unless @universe.nil? %>
          <%= render partial: "shared/navbar_link", locals: {
            label: "Edit Universe",
            url: edit_universe_url(@universe),
          } %>
        <% end %>
        <% unless @character.nil? %>
          <%= render partial: "shared/navbar_link", locals: {
            label: "Edit Character",
            url: edit_character_url(@character),
          } %>
        <% end %>
        <% unless @location.nil? %>
          <%= render partial: "shared/navbar_link", locals: {
            label: "Edit Location",
            url: edit_location_url(@location),
          } %>
        <% end %>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Register", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
